<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="info.papdt.lolistat">

	<application
		android:allowBackup="true"
		android:icon="@drawable/pop_icon"
		android:label="@string/app_name">
		
		<activity
			android:name=".ui.base.GlobalActivity"
			android:title="@string/app_name"
			android:theme="@style/AppTheme">
			
			<intent-filter>
				<category android:name="android.intent.category.LAUNCHER"/>
				<action android:name="android.intent.action.MAIN"/>
			</intent-filter>
			
		</activity>
		
		<activity
			android:name=".ui.settings.BlackListActivity"
			android:label="@string/blacklist"
			android:theme="@android:style/Theme.Material">

			<intent-filter>
				<category android:name="android.intent.category.DEFAULT"/>
				<action android:name="android.intent.action.MAIN"/>
			</intent-filter>

		</activity>
		
		<meta-data
			android:name="xposedmodule"
			android:value="true"/>
		<meta-data
			android:name="xposeddescription"
			android:value="Hack to match the status bar style on Lollipop to match app"/>
		<meta-data
			android:name="xposedminversion"
			android:value="30"/>
	</application>

</manifest>
